#!/usr/bin/env zsh

if [[ -d "$HOME/anaconda3" ]];then
    export PATH="$HOME/anaconda3/bin:$PATH"
fi
if [[ -d "$HOME/Library/Frameworks/flutter/bin" ]]; then
	export PATH="$HOME/Library/Frameworks/flutter/bin:$PATH"
fi
